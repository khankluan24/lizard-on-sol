interface WhiteList {
    _id: string
    address: string
    currentbuy: number
    lastsign?: string[]
}

interface TotalRise {
    _id: string
    address: string
    currentbuy: number
    lastsign?: string[]
}